# Project Instructions

## Global Documentation Rule
- All project documentation must be written in English.

## Build and Run Rule
- For this project, always run compilation and execution commands with release target (for example: `cargo build --release`, `cargo run --release`, `cargo test --release` when applicable).
